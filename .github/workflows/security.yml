name: Security Scan

on:
  push:
    branches: [master]
permissions:
  contents: read

jobs:
  gitleaks:
    uses: juninmd/base-actions/.github/workflows/reusable-security-scan.yml@fc51fbba3fb79c24567294e1871eb12a451feeba
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
