name: Validate
on:
  pull_request:
  push:
    branches: [master]
concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check:
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    uses: juninmd/base-actions/.github/workflows/reusable-validate.yml@fc51fbba3fb79c24567294e1871eb12a451feeba
    with:
      python-version: ${{ matrix.python-version }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}